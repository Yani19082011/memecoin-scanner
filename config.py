import os
from dotenv import load_dotenv

load_dotenv()


def _bool(name: str, default: bool = False) -> bool:
    val = os.getenv(name)
    if val is None:
        return default
    return val.strip().lower() in ("1", "true", "yes", "on")


PUMPPORTAL_WS_URL = os.getenv("PUMPPORTAL_WS_URL", "wss://pumpportal.fun/api/data")

# --- Реално-времево следене вместо еднократна проверка след фиксирано изчакване ---
# Изчакване DexScreener да индексира новия pool (обикновено 1-5 мин, виж README)
INITIAL_INDEX_DELAY_SECONDS = int(os.getenv("INITIAL_INDEX_DELAY_SECONDS", "90"))
# На всеки колко секунди се проверява всяка следена монета
POLL_INTERVAL_SECONDS = int(os.getenv("POLL_INTERVAL_SECONDS", "60"))
# Колко минути общо следим монетата, преди да се откажем (ако не пресече прага)
MONITOR_WINDOW_MINUTES = int(os.getenv("MONITOR_WINDOW_MINUTES", "45"))

# --- Защита срещу "купуване на върха" ---
# Потребителска обратна връзка (17.09): 3 алърта, при които монетата вече е
# била на локален връх/вече паднала точно когато алъртът е пристигнал
# ("покачи се съвсем малко за секунда и после падна", "секунди след като
# ми го прати койна беше вече мъртъв"). Причината: преди пращахме алърт
# веднага щом score-ът пресече прага на ПЪРВАТА проверка (poll #1) -
# това хваща и монети, които тъкмо в този миг вече са на върха на
# кратък spike и обръщат надолу. Двете защити по-долу изискват алъртът
# да е ПОТВЪРДЕН (не еднократен spike) и монетата да НЕ е вече паднала
# осезаемо от най-високата видяна цена, преди да пратим email:
#   1. MIN_POLLS_BEFORE_ALERT - не пращай на самия първи poll, изчакай
#      поне толкова последователни проверки над прага.
#   2. PEAK_DRAWDOWN_STOP_PCT - ако цената вече е паднала с този % от
#      пика, откакто следим монетата, приемаме че вече сме изпуснали
#      върха и НЕ пращаме (просто продължаваме да следим тихо).
# Компромис: добавя ~POLL_INTERVAL_SECONDS допълнително забавяне преди
# първия възможен алърт - по избор на потребителя ("опитвай се да не
# намираш повече такива"), приемаме да пропуснем някой и друг бърз мувър,
# за да намалим лошите/мъртви алърти.
MIN_POLLS_BEFORE_ALERT = int(os.getenv("MIN_POLLS_BEFORE_ALERT", "3"))
PEAK_DRAWDOWN_STOP_PCT = float(os.getenv("PEAK_DRAWDOWN_STOP_PCT", "10"))
# (17.09) Потребителят първо поиска по-чест поток (~1/5 мин), после уточни
# приоритета: по-важно е монетата да НЕ е вече мъртва, когато я отвори,
# отколкото честотата - "може и 1 на 7-8 минути, просто да не е ръгпълната
# в следващите 10 секунди". Затова оставаме на 3 последователни потвърждения
# (не 2) - плюс виж FINAL_CHECK_BEFORE_SEND по-долу и коментара в
# main.py::monitor_token за нова защита точно в последния момент преди
# изпращане.

# На всеки колко проверки да опресняваме RugCheck доклада наново (не само
# докато е бил празен) - виж коментара в main.py::monitor_token за реалния
# rug pull случай (17.09), който показа че стар/остарял RugCheck доклад
# може да продължи да изглежда "чист" дълго след като монетата реално се е
# влошила (нова Low Liquidity/insider флагове, паднал LP lock %).
RUGCHECK_REFRESH_EVERY_N_POLLS = int(os.getenv("RUGCHECK_REFRESH_EVERY_N_POLLS", "2"))

# --- Защита срещу "liquidity rug" (LP не е заключен) ---
# Реален случай (17.09): монета score=76, mint authority revoke-нат, freeze
# authority revoke-нат, 0 high-severity RugCheck флага - изглеждаше чиста,
# но rug pull-na и потребителят загуби всичко. Причината, видима едва след
# ръчна проверка на пълния RugCheck доклад: монетата имаше 2 pool-а -
# единият 100% locked, НО покриваше само 2.75% от реалната ликвидност;
# другият (97.25% от резерва) беше 0% locked. RugCheck-ският "risks" масив
# изобщо НЕ флагна това (само 2 "warn" флага за друго) - трябва директно
# да проверим markets[].lp.lpLockedPct, претеглено по markets[].lp.pctReserve,
# защото ИМЕННО там е реалният rug вектор: собственикът може да изтегли
# незаключената ликвидност по всяко време. Ако претеглената % заключена
# ликвидност е под MIN_LP_LOCKED_PCT, ТВЪРДО отхвърляме монетата (score=0),
# независимо от другите фактори - виж scoring.py::_weighted_lp_locked_pct.
MIN_LP_LOCKED_PCT = float(os.getenv("MIN_LP_LOCKED_PCT", "50"))

# --- Твърд блок при explicit "Low Liquidity" RugCheck риск ---
# Реален сравнителен тест (17.09) на 5 живи изхода: 2-те rug-нали монети
# бяха ЕДИНСТВЕНИТЕ от 5-те с RugCheck-ски "Low Liquidity" риск в списъка
# ("danger" за едната, дори само "warn" за другата) - нито добрите, нито
# "лошата, но не rug" монета го имаха. Затова: наличие на този риск (на
# КАКЪВТО и да е level, дори "warn") -> твърд блок, независимо от score-а -
# виж scoring.py::score_token.
BLOCK_ON_LOW_LIQUIDITY_RISK = os.getenv("BLOCK_ON_LOW_LIQUIDITY_RISK", "true").strip().lower() in ("1", "true", "yes", "on")

# --- Твърд блок при екстремна insider концентрация ---
# Реален случай (17.09): TRUPAI ("Trump Paid") - RugCheck откри 33 insider
# wallet клъстъра (5 отделни мрежи, до 9 wallet-а в една). За сравнение SVEN
# (легитимно добра монета, +518%) имаше само 4 клъстъра - затова преди
# третирахме graphInsidersDetected само информативно (виж scoring.py). 33
# обаче е категорично различен мащаб от "няколко клъстъра" - при такава
# концентрация shte блокираме директно, независимо от другите фактори. Праг
# 10 е избран съзнателно високо над SVEN-ското 4 (за да не хващаме случайно
# легитимни монети с по няколко клъстъра), но много под TRUPAI-ското 33.
MAX_INSIDER_CLUSTERS = float(os.getenv("MAX_INSIDER_CLUSTERS", "10"))

# --- Dev/creator "история" (24.09, по избор на потребителя: "да гледа
# историята на deva на койна") ---
# ЧЕСТНА БЕЛЕЖКА, преди всичко: RugCheck-ският ЕДИНИЧЕН token report вече
# дава 'creator' (wallet адреса на този, който е пуснал монетата) и
# 'creatorBalance' - тях просто ги ПОКАЗВАМЕ в имейла (виж scoring.py), за да
# можеш сам да провериш wallet-а в Solscan/RugCheck, ако искаш. RugCheck-ският
# ДРУГ endpoint - история на ВСИЧКИ токени, пуснати от този wallet - НЕ е
# официално документиран публично, кодът пробва разумен URL (виж
# data_sources.py::get_creator_history), но ако RugCheck е сменил схемата/
# пътя, просто ще получаваме тихо "няма данни" (виж коментара там) - това е
# ДОПЪЛНИТЕЛНА, best-effort информация, НЕ твърд блок (за разлика от
# graphInsidersDetected/MAX_INSIDER_CLUSTERS по-горе, който Е официално
# поле в самия token report). Ако някой ден провериш логовете и видиш, че
# работи стабилно, кажи ми, за да го превърнем в твърд блок.
MAX_CREATOR_PRIOR_TOKENS = float(os.getenv("MAX_CREATOR_PRIOR_TOKENS", "5"))

# --- Твърд блок при концентрация на притежателите (holder concentration) ---
# (24.09, по изричен избор на потребителя, след реален пример: монета с $2K
# market cap, само 36 holders, топ 10 държаха 99.92% от целия supply - т.е.
# буквално 1-2 wallet-а могат да съборят цената по всяко време, без изобщо
# да е "insider клъстър" в RugCheck-ския граф-смисъл по-горе). Тук ползваме
# директно суровите % дялове от RugCheck-ския 'topHolders' масив (различно
# от graphInsidersDetected/MAX_INSIDER_CLUSTERS - това следи ПОЗНАТИ/свързани
# insider мрежи, а не просто "колко % държи най-едрият wallet"):
#   - MAX_TOP_HOLDER_PCT: топ 1 (най-едрият единичен) wallet не бива да
#     държи повече от този % от supply-а сам по себе си ("клъстър на първо
#     място").
#   - MAX_TOP10_HOLDERS_PCT: сборът от топ 10 wallet-а не бива да надвишава
#     този %.
# И двете са ТВЪРД блок (score=0), не просто наказателни точки - концентра-
# цията е структурен риск, не се компенсира с добри пазарни числа (същата
# логика като danger risk/mint-freeze authority блоковете по-горе). Ако
# RugCheck още няма 'topHolders' данни за монетата (твърде нова) - НЕ
# блокираме (недостатъчно данни), само информативна бележка в имейла.
MAX_TOP_HOLDER_PCT = float(os.getenv("MAX_TOP_HOLDER_PCT", "10"))
MAX_TOP10_HOLDERS_PCT = float(os.getenv("MAX_TOP10_HOLDERS_PCT", "20"))

# Вдигнато от 5000 на 15000 (17.09) - същият сравнителен тест показа ясен
# градиент: 2-те rug-нали монети имаха най-плитка обща ликвидност ($2,366 и
# $5,058), "лошата, но не rug" монета - средно ($16,360), двете добри - най-
# дълбоко ($33,075 и $92,309). Плитка ликвидност е по-лесна за източване и с
# по-малко нужен капитал за манипулация - фундаментален признак за риск, не
# просто случайност в тази извадка.
MIN_LIQUIDITY_USD = float(os.getenv("MIN_LIQUIDITY_USD", "15000"))
# Горен праг на market cap - за да хващаме монети РАНО, преди да са
# набъбнали много. Монета над този таван никога не се score-ва/алъртва,
# независимо от другите фактори.
#
# ПРОМЯНА (18.09 вечерта, НОВА СТРАТЕГИЯ по изричен избор на потребителя:
# "да не е над 120k mc") - махнахме старата двустепенна система (обичаен
# таван 110k + отделна "разширена зона" до 500k за монети с абсолютно чист
# RugCheck доклад). Вместо това: ЕДИН твърд таван, без изключения.
#
# ЧЕСТЕН КОМПРОМИС, който потребителят трябва да знае: старата "разширена
# зона" съществуваше заради реален случай (TIGRINO, 91ryaC...Lgpump, 18.09) -
# купено на $170.5K MC, продадено на $220.1K MC за +29%, но монетата
# продължи ДО $1.5M MC (+52,081% от старта) - старата логика позволяваше да
# продължим да следим точно такива случаи отвъд обичайния праг. Новият твърд
# 120k таван значи ботът систематично НЯМА да продължи да следи монети като
# TIGRINO отвъд тази граница - директен резултат от изричното желание за
# по-прост, по-строг праг, не пропуск/бъг.
# ПРОМЯНА (24.09, по НОВ изричен избор на потребителя - иска да хваща монети
# "преди да изригнат", изрично спомена "100к или 200к mc" - виж EARLY_GEM_*
# по-долу за новата отделна "ранна монета" функция, която точно за това е) -
# вдига тавана обратно до 200k (над старите 120k от 18.09 вечерта). Старият
# коментар за TIGRINO компромиса по-горе остава като исторически контекст -
# новото желание директно го суперсижда: потребителят иска точно зоната
# 120k-200k, която старият таван изключваше.
MAX_MARKET_CAP_USD = float(os.getenv("MAX_MARKET_CAP_USD", "200000"))

# Максимално разумно съотношение обем(1ч)/ликвидност - над това се приема за
# съмнително (изкуствено изпомпан/wash-traded обем), не органичен интерес.
# Реален случай (18.09): 6jUqDqQid...pump - $418,874 обем/1ч срещу само
# $7,389 ликвидност (~57x) точно преди rug pull - явно изкуствено. Здравите
# монети в сравнителните тестове досега никога не са доближавали такова
# съотношение.
MAX_VOLUME_TO_LIQUIDITY_RATIO = float(os.getenv("MAX_VOLUME_TO_LIQUIDITY_RATIO", "15"))

# --- НОВА СТРАТЕГИЯ (18.09 вечерта) - истински "тренд" сигнали ---
# По изричен избор на потребителя: "искам да търсиш тренд ... наистина
# потърси информация доста време нова стратегия" - вместо просто да върнем
# старото монотонно точкуване (liquidity/volume/momentum), направихме реално
# проучване (официалната DexScreener API схема, публични rug-pull red-flag
# ръководства, и една data-driven, но НЕ рецензирана статия за pump.fun
# поведение) за нови сигнали от данни, които вече ползваме безплатно, но до
# сега НЕ извличахме от best_pair: txns (брой купувания/продажби по
# timeframe), priceChange, pairCreatedAt (кога се е появил пула).
#
# ЧЕСТНА БЕЛЕЖКА за качеството на източниците: DexScreener схемата (txns.
# {m5,h1,h6,h24}.{buys,sells}, priceChange, pairCreatedAt) е потвърдена от
# официалната им документация - надеждно. "Първите ~6 часа са най-добрият
# прозорец" идва от ЕДНА нерецензирана статия с data-driven твърдения, не
# научно изследване - третираме го като разумна евристика, не факт. Намерена
# е и академична статия за pump.fun "graduation" prediction, но тя мери
# различен въпрос (дали токен ще graduate-не, не дали ще оцелее СЛЕД това) -
# затова не ползваме директно нейния модел/тегла, само общата идея, че
# ранната търговска активност носи реален сигнал.
#
# ВАЖНО - DexScreener НЯМА безплатно поле за уникален брой holder-и/wallet-и
# (потвърдено при проучването) - затова "buy pressure" по-долу (съотношение
# купувания/продажби) е най-близкият безплатен proxy за органичен интерес,
# не директно броене на нови държатели.

# "Buy pressure" - дял купувания от общия брой сделки (купувания+продажби)
# за последния 1ч. Над този праг -> пълен бонус в точкуването (виж
# scoring.py::_buy_pressure_points).
MIN_BUY_RATIO_FOR_FULL_TREND_BONUS = float(os.getenv("MIN_BUY_RATIO_FOR_FULL_TREND_BONUS", "0.65"))

# "Early stage" бонус - колко часа от появата на пула (pairCreatedAt) все
# още се брои за "прясна" възможност (пълен бонус до EARLY_STAGE_MAX_AGE_
# HOURS, после линейно избледнява до 0 на EARLY_STAGE_FADE_AGE_HOURS - НЕ
# наказва по-старите монети, просто спира да ги награждава допълнително за
# "свежест").
EARLY_STAGE_MAX_AGE_HOURS = float(os.getenv("EARLY_STAGE_MAX_AGE_HOURS", "6"))
EARLY_STAGE_FADE_AGE_HOURS = float(os.getenv("EARLY_STAGE_FADE_AGE_HOURS", "12"))

# "Death spiral" - НОВ, умишлено ТЕСЕН твърд блок (различен от съществуващия
# MAX_VOLUME_TO_LIQUIDITY_RATIO блок по-горе): ако в последните 5 минути има
# 0 купувания, но поне DEATH_SPIRAL_MIN_M5_SELLS продажби, И цената вече е
# паднала с поне DEATH_SPIRAL_H1_DROP_PCT% през последния час - активен dump
# в момента, не просто "слаб момент" - твърдо пропускаме, независимо от
# score-а. Умишлено изисква И трите условия едновременно, за да не пресъздаде
# проблема от "sweet spot" експеримента (18.09 сутринта) - часове наред нито
# един алърт от прекалено агресивно филтриране.
DEATH_SPIRAL_H1_DROP_PCT = float(os.getenv("DEATH_SPIRAL_H1_DROP_PCT", "20"))
DEATH_SPIRAL_MIN_M5_SELLS = int(os.getenv("DEATH_SPIRAL_MIN_M5_SELLS", "3"))

# "Wash trading по размер на сделка" - МЕК наказателен сигнал (само отнема
# точки, НЕ твърд блок - за разлика от MAX_VOLUME_TO_LIQUIDITY_RATIO по-горе).
# Среден размер на сделка = обем(1ч) / брой сделки(1ч). Нормалната pump.fun
# memecoin търговия е много дребни ($10-$500) сделки от много отделни хора -
# необичайно ГОЛЯМ среден размер на сделка е индикация за малко на брой,
# изкуствени сделки, надуващи обема, вместо истински широк интерес. Мек
# сигнал, защото само по себе си не е доказан rug vector като LP lock/mint
# authority - затова наказва точки, не блокира изцяло.
SUSPICIOUS_AVG_TRADE_SIZE_USD = float(os.getenv("SUSPICIOUS_AVG_TRADE_SIZE_USD", "3000"))
WASH_TRADE_SIZE_PENALTY_POINTS = float(os.getenv("WASH_TRADE_SIZE_PENALTY_POINTS", "10"))

HIGH_POTENTIAL_THRESHOLD = float(os.getenv("HIGH_POTENTIAL_THRESHOLD", "65"))

# --- Финална проверка "в последната секунда" точно преди изпращане ---
# (17.09) Потребителят иска преди всичко монетата да НЕ е вече мъртва, когато
# я отвори - дори секунди преди/след изпращането на алърта. Дотук решението
# "да пратим" се вземаше от данните на САМИЯ poll, който го задейства - но
# между този poll и реалното изпращане може вече да е минал момент. Затова
# main.py::monitor_token сега прави ОЩЕ ЕДНА, съвсем свежа live проверка
# (директна DexScreener заявка, не кеша) точно преди send_alert() - ако
# цената вече е паднала над този праг спрямо пика МЕЖДУ последното
# потвърждение и този момент, алъртът се отменя в последния момент, вместо
# да пратим нещо вече мъртво.
FINAL_CHECK_MAX_DRAWDOWN_PCT = float(os.getenv("FINAL_CHECK_MAX_DRAWDOWN_PCT", "8"))

ALERT_EMAIL_ENABLED = _bool("ALERT_EMAIL_ENABLED", False)
# --- Resend (https://resend.com) - изпраща email през HTTP API, не през Gmail SMTP.
# Ползваме го вместо Gmail App Password, защото Family Link/supervised Google
# акаунти не позволяват App Passwords изобщо. Виж README.md за регистрация.
RESEND_API_KEY = os.getenv("RESEND_API_KEY", "")
RESEND_FROM_EMAIL = os.getenv("RESEND_FROM_EMAIL", "onboarding@resend.dev")
# ВАЖНО (23.09, намерено в живи Render логове веднага след първия реален
# алърт): пробвахме ALERT_EMAIL_TO с 2 адреса (по избор на потребителя,
# "искам да изпраща на 2 отделни имейла") - Resend го отказа НАПЪЛНО с
# HTTP 403: "You can only send testing emails to your own email address
# (yani.kolev2011@gmail.com). To send emails to other recipients, please
# verify a domain at resend.com/domains...". Причината: безплатният Resend
# "sandbox" режим (без верифициран собствен домейн) позволява изпращане
# САМО до имейла, с който е регистриран акаунтът - никакви други адреси,
# независимо дали са в 'to' списъка на един email или в отделни заявки.
# Затова ALERT_EMAIL_TO се връща на ЕДИН адрес (иначе ВСИЧКИ email-и,
# включително истинските потвърдени алърти, спират да излизат). Вторият
# адрес (crafts0man0@gmail.com) вижте RESEND_API_KEY_2/ALERT_EMAIL_TO_2
# по-долу - отделен, безплатен Resend акаунт е единственият начин да се
# праща и до втори адрес БЕЗ да се купува/верифицира собствен домейн.
ALERT_EMAIL_TO = os.getenv("ALERT_EMAIL_TO", "yani.kolev2011@gmail.com")

# --- Втори получател (по избор) - изисква ОТДЕЛЕН, собствен Resend акаунт ---
# Всеки безплатен Resend акаунт (без верифициран домейн) може да праща
# email само до адреса, с който е регистриран - виж бележката по-горе.
# За да стигат известията и до ВТОРИ адрес безплатно, трябва ВТОРИ Resend
# акаунт, регистриран директно с този втори имейл (напр. crafts0man0@
# gmail.com), с негов СОБСТВЕН API ключ тук. Ако RESEND_API_KEY_2 остане
# празен - вторият адрес просто се пропуска мълчаливо (не гърми нищо).
RESEND_API_KEY_2 = os.getenv("RESEND_API_KEY_2", "")
ALERT_EMAIL_TO_2 = os.getenv("ALERT_EMAIL_TO_2", "crafts0man0@gmail.com")

# --- Anti-spam защита за Resend дневната квота (безплатен tier = 100 имейла/ден
# за целия акаунт, споделен с другите ботове, ползващи същия RESEND_API_KEY) ---
# Не пращаме имейл по-често от веднъж на MIN_EMAIL_INTERVAL_SECONDS (300с = 5мин,
# значи максимум ~2 имейла на всеки 10 минути), плюс твърд дневен таван като
# резерва под истинския лимит на Resend. Алъртът винаги се вижда в Render Logs -
# само самото email изпращане се прескача, ако сме над темпото.
#
# ВАЖНО (17.09) - таван на Resend безплатния план: 300с = точно "1 имейл на
# 5 мин" темпо, каквото поиска потребителят - това само по себе си НЕ е
# проблем. Реалният таван е MAX_EMAILS_PER_DAY по-долу + споделеният
# Resend лимит от 100 имейла/ден за ЦЕЛИЯ акаунт (двата бота заедно). Ако
# се пращаше буквално 1 имейл на 5 мин през целия активен прозорец
# (07:30-23:00 = 15.5ч), това би било ~186 имейла САМО за memecoin бота -
# почти 2x над целия безплатен Resend лимит. Тоест "1 на 5 мин" е горен
# ТЕМП (максимална честота), не гарантирана устойчива честота цял ден -
# реалната честота зависи и от това колко монети реално минават филтрите.
# Ако искаш буквално устойчиво 1 на 5 мин по цял ден, ще трябва платен
# Resend план (безплатният физически не го побира) - кажи, ако искаш да
# видим цената.
MIN_EMAIL_INTERVAL_SECONDS = int(os.getenv("MIN_EMAIL_INTERVAL_SECONDS", "300"))

# --- Периодичен "heartbeat" email (18.09 вечерта, по изричен избор на
# потребителя: "изпраща ми имейл на всеки 5 минути за койн" - независимо
# дали вече е напълно потвърдена, "не да седи на един") ---
# За разлика от обичайния алърт (само когато score мине HIGH_POTENTIAL_
# THRESHOLD и се потвърди с MIN_POLLS_BEFORE_ALERT последователни проверки),
# тук пращаме НАЙ-ДОБРАТА в момента следена монета на всеки
# MIN_EMAIL_INTERVAL_SECONDS, дори да не е напълно потвърдена - ясно
# обозначено в имейла като "периодично, не напълно потвърдено". Използва
# СЪЩОТО anti-spam темпо/дневен таван (notifier.can_send_now()) - ако вече
# е пратен истински потвърден алърт в тази 5-минутна пауза, heartbeat-ът
# просто се прескача този цикъл (не удвоява имейлите).
WATCH_DIGEST_ENABLED = _bool("WATCH_DIGEST_ENABLED", True)
# ПРОМЯНА (24.09, по изричен избор на потребителя: "искам да го направим да
# ми изпраща през 10 минути койн") - ОТДЕЛЕН интервал от MIN_EMAIL_INTERVAL_
# SECONDS (който продължава да управлява само anti-spam темпото на истинските
# ВИСОК ПОТЕНЦИАЛ алърти) - преди digest loop-ът буквално спеше за
# MIN_EMAIL_INTERVAL_SECONDS, което объркваше двете различни неща (честота на
# heartbeat-а vs. минимална пауза между истински алърти) в една настройка.
WATCH_DIGEST_INTERVAL_SECONDS = int(os.getenv("WATCH_DIGEST_INTERVAL_SECONDS", "600"))
# ПРОМЯНА (23.09, по изричен избор на потребителя: "искам да ми дава само
# монети със score над 50, нали да си заслужава" - оплакване, че от 30
# heartbeat имейла само 1 е имал score 50, останалите дори 0) - ПРИЧИНАТА:
# _watch_digest_loop() в main.py пращаше БЕЗУСЛОВНО "най-добрата в момента
# следена" монета на всеки MIN_EMAIL_INTERVAL_SECONDS, дори когато буквално
# НИЩО прилично не се следи (score 0-20) - "най-добра от лош избор" пак се
# пращаше. Сега digest-ът просто ПРЕСКАЧА цикъла (изчаква следващия, вместо
# да прати боклук), ако най-добрият текущ score е под този праг.
WATCH_DIGEST_MIN_SCORE = float(os.getenv("WATCH_DIGEST_MIN_SCORE", "50"))
# ПРОМЯНА (24.09, по оплакване на потребителя: "последните 3 имейла за
# memecoins е за един и същи койн") - ПРИЧИНАТА: ако една и съща монета си
# остане "най-добрата в момента следена" за няколко поредни 5-минутни цикъла
# (напр. следи се дълго, score стабилен, но никога не се потвърждава),
# digest-ът я пращаше ОТНОВО всеки път - технически "прясна проверка", но за
# потребителя изглежда като спам за нещо, което вече е видял. Сега: ако
# digest-ът щеше да прати ПАК СЪЩАТА монета като последния път, пропускаме
# цикъла, освен ако не е минал поне WATCH_DIGEST_REPEAT_COOLDOWN_MINUTES -
# т.е. същата монета пак ще получи "напомняне" (не е изчезнала/влошила се),
# просто по-рядко, вместо на всеки 5 мин.
WATCH_DIGEST_REPEAT_COOLDOWN_MINUTES = int(os.getenv("WATCH_DIGEST_REPEAT_COOLDOWN_MINUTES", "30"))

# --- "Ранна монета" (early gem) - ОТДЕЛЕН, по-рядък, по-задълбочен сигнал
# (24.09, по изричен избор на потребителя: "да открива койнове преди да
# изригнат, примерно дава ми го на 100к или 200к mc, и то стига 2-3 mil mc" -
# "там по-сериозен research - пак dev на койна, новини hype, rugpull check").
#
# КАК РАБОТИ: за разлика от digest-а (показва каквото в момента е "най-
# добро", независимо от market cap) и обичайния ВИСОК ПОТЕНЦИАЛ алърт (чака
# потвърден моментум), тук следим дали монета, докато я следим на живо, влезе
# в диапазона [EARLY_GEM_MIN_MC, EARLY_GEM_MAX_MC] СЪС score над прага -
# изпращаме ВЕДНАГА (не чакаме няколко последователни потвърждения - целта е
# скорост, да те хване РАНО), но само ВЕДНЪЖ на монета (виж main.py
# _early_gem_alerted). Резултатът вече минава ВСИЧКИ съществуващи твърди
# защити на score_token() (top holder концентрация, mint/freeze authority,
# danger risk флагове, insider клъстъри, LP lock, ликвидност) - "по-сериозен
# research" тук означава допълнително: creator/dev wallet адрес (за да го
# провериш сам - виж scoring.py за честната бележка защо няма напълно
# автоматична "история на dev-a" проверка) + дали монетата изобщо има
# социални линкове (Twitter/Telegram/website от DexScreener - грубо, но
# безплатно proxy за "hype"/community усилие).
#
# ЧЕСТНА БЕЛЕЖКА: "стига 2-3 mil mc" НЕ е гаранция нито прогноза - никой
# безплатен инструмент не може да предскаже това с точност. Прагът/диапазонът
# тук просто засича монети РАНО в жизнения им цикъл, докато все още е евтино
# да се влезе, СЪС профил (ликвидност/обем/buy pressure/чист RugCheck), който
# исторически корелира с по-нататъшен ръст - не повече от това.
EARLY_GEM_ENABLED = _bool("EARLY_GEM_ENABLED", True)
EARLY_GEM_MIN_MC = float(os.getenv("EARLY_GEM_MIN_MC", "100000"))
EARLY_GEM_MAX_MC = float(os.getenv("EARLY_GEM_MAX_MC", "200000"))
# По-нисък от обичайния HIGH_POTENTIAL_THRESHOLD - нарочно, за да хванем
# монетата РАНО, преди пълно потвърден моментум (иначе просто щеше да чакаме
# обичайния ВИСОК ПОТЕНЦИАЛ алърт, не по-рано).
EARLY_GEM_MIN_SCORE = float(os.getenv("EARLY_GEM_MIN_SCORE", "50"))
# Ако True - изисква поне един социален линк (Twitter/Telegram/website) от
# DexScreener, за да пратим - по-строг "hype" филтър, но може да пропусне
# добри монети, които просто още не са попълнили тази информация. По
# подразбиране False (само информативно в имейла, не блокира).
EARLY_GEM_REQUIRE_SOCIAL_PRESENCE = _bool("EARLY_GEM_REQUIRE_SOCIAL_PRESENCE", False)
# Дневната квота (100 общо за акаунта) е разпределена 70/30 между двата бота
# по избор на потребителя (18.09: "70 memecoins 30 pennystocks") - тук 70,
# penny-stock-scanner държи 30 (виж неговия config.py).
MAX_EMAILS_PER_DAY = int(os.getenv("MAX_EMAILS_PER_DAY", "70"))

# --- "Тихи часове" за имейл алъртите - потребителят иска имейли САМО между
# 07:30 и 23:00 местно време (не иска да го буди бот през нощта). За разлика
# от penny-stock-scanner-а, memecoin ботът следи pump.fun 24/7 (crypto пазарът
# никога не спира) - затова тук границата реално има значение, не е просто
# резерва. Алъртите пак се логват в Render Logs денонощно - само самото
# изпращане на email се пропуска извън тези часове. ---
ALERT_QUIET_HOURS_TZ = os.getenv("ALERT_QUIET_HOURS_TZ", "Europe/Sofia")
ALERT_ACTIVE_START_HOUR = int(os.getenv("ALERT_ACTIVE_START_HOUR", "7"))
ALERT_ACTIVE_START_MINUTE = int(os.getenv("ALERT_ACTIVE_START_MINUTE", "30"))
ALERT_ACTIVE_END_HOUR = int(os.getenv("ALERT_ACTIVE_END_HOUR", "23"))
ALERT_ACTIVE_END_MINUTE = int(os.getenv("ALERT_ACTIVE_END_MINUTE", "0"))

# --- Impersonation филтър ---
# pump.fun монети, кръстени на известни хора/личности (Elon Musk, Trump и
# т.н.) practически НИКОГА не са реално създадени от въпросния човек - това
# е чест "hype" trick за rug pull. Понеже няма начин да потвърдим реална
# автентичност през безплатни API-та, монета чието име/символ съвпада с
# някое от тези ключови думи автоматично се пропуска (score=0), вместо да
# разчитаме на моментум/ликвидност да я скрият случайно.
IMPERSONATION_KEYWORDS = [
    kw.strip().lower()
    for kw in os.getenv(
        "IMPERSONATION_KEYWORDS",
        "elon,elonmusk,musk,spacex,neuralink,"
        "trump,donaldtrump,maga,"
        "biden,joebiden,kamala,kamalaharris,"
        "obama,barackobama,putin,zelensky,zelenskyy,"
        "kanye,yeezy,drake,kimkardashian,kardashian,"
        "messi,ronaldo,neymar,"
        "mrbeast,"
        "bezos,jeffbezos,zuckerberg,markzuckerberg,gates,billgates,"
        "buffett,warrenbuffett,cathiewood,sbf,sambankmanfried,"
        "taylorswift,beyonce,rihanna,kimjongun,"
        "vitalik,vitalikbuterin,buterin,satoshi,cz_binance,changpeng,zhao,"
        "justinsun,tron",
    ).split(",")
    if kw.strip()
]

# Само име на известна личност в тикъра НЕ е достатъчно, за да пропуснем
# монетата - "PAIDLON", "WIFELON", "$SUNGE" (Justin Sun Prize) са класически
# pump.fun meme/joke имена, НЕ твърдят реална връзка, и реално потребителят
# потвърди че такива монети са били добри (не instant rug, една дори +518%).
# Затова блокираме само когато ИМЕТО НА ЛИЧНОСТ + ДУМА ЗА "легитимност/
# официалност" се появят заедно - това е истинският сигнал за измама, която
# твърди реална връзка с човека (напр. "Elon Musk Official", "Real Trump
# Coin"), не обикновена meme препратка.
IMPERSONATION_LEGITIMACY_WORDS = [
    kw.strip().lower()
    for kw in os.getenv(
        "IMPERSONATION_LEGITIMACY_WORDS",
        "official,verified,genuine,authentic,foundation,realaccount,"
        "confirmed,endorsed,ceo",
    ).split(",")
    if kw.strip()
]

PORT = int(os.getenv("PORT", "10000"))

# --- Self-ping (keep-alive) ---
# Виж коментара в main.py::_self_ping_loop за защо съществува (18.09, по
# оплакване "от час и нещо няма никакви сигнали" - Render безплатният план
# приспива service-а след 15 мин без входящ трафик). 5 минути = 3x резерва
# спрямо 15-те минути праг, вместо да разчитаме само на външен pinger.
KEEP_ALIVE_PING_MINUTES = int(os.getenv("KEEP_ALIVE_PING_MINUTES", "5"))

# --- Трайна памет отвъд Render локалния диск (по избор) ---
# Виж коментара в seen_store.py::load_seen / PennyStockScanner/config.py за
# пълния контекст (18.09). Ако зададени - "seen" списъкът се пази в
# безплатен Upstash Redis (upstash.com) вместо на локалния Render диск,
# който се изтрива при всеки redeploy. Взимат се от Upstash dashboard ->
# базата -> таб "REST API". Ако останат празни - старо поведение с локален
# файл. Може да се ползва СЪЩАТА Upstash база и за PennyStockScanner -
# ключовете са различни, не се бъркат.
UPSTASH_REDIS_REST_URL = os.getenv("UPSTASH_REDIS_REST_URL", "")
UPSTASH_REDIS_REST_TOKEN = os.getenv("UPSTASH_REDIS_REST_TOKEN", "")

DATA_DIR = os.path.join(os.path.dirname(__file__), "data")
SEEN_FILE = os.path.join(DATA_DIR, "seen.json")
